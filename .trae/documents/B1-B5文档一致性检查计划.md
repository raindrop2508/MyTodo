# B1-B5 文档与实际实现一致性检查计划

## 一、检查结论

通过对比 `StageB_核心流程跑通计划.md` 与实际代码实现，发现以下不一致点：

### 1. B1 领域模型定义不一致

| 计划中提到的文件 | 实际是否存在 | 状态 |
|----------------|------------|------|
| `domain/Task.kt` | ✅ 存在 | 一致 |
| `domain/TaskStep.kt` | ✅ 存在 | 一致 |
| `domain/Category.kt` | ✅ 存在 | 一致 |
| `domain/TaskType.kt` | ✅ 存在 | 一致 |
| `domain/TaskStatus.kt` | ✅ 存在 | 一致 |
| `domain/StepStatus.kt` | ✅ 存在 | 一致 |
| `domain/PomodoroSession.kt` | ✅ 存在 | **计划中未提及** |
| `domain/SessionStatus.kt` | ✅ 存在 | **计划中未提及** |

**差异说明**：计划中 B1 仅定义了 6 个领域模型文件，但实际实现中多了 `PomodoroSession.kt` 和 `SessionStatus.kt`（用于番茄钟会话）。

### 2. B2-B3 完全一致

- Repository 接口文件（TaskRepository、CategoryRepository、PomodoroRepository、TaskQuery）与计划完全一致
- FakeRepository 实现文件（FakeTaskRepository、FakeCategoryRepository）与计划完全一致

### 3. B4-B5 尚未开始

- ViewModel 层尚未创建
- Mock 数据迁移尚未执行

---

## 二、修改方案

### 修改文件 1：`StageB_核心流程跑通计划.md`

**B1 部分修改**：
- 在"影响文件"列表中增加 `domain/PomodoroSession.kt` 和 `domain/SessionStatus.kt`
- 更新验证标准，确认额外实体的类型约束符合要求

**B2 部分修改**：
- 在"影响文件"中确认 PomodoroRepository 接口已创建（与计划一致）

### 修改文件 2：`领域数据模型定义.md`

- 文件内容与实际实现完全一致，无需修改

### 修改文件 3：`TaskRepository 接口定义计划.md`

- 文件内容与实际实现完全一致，无需修改

### 修改文件 4：`数据流转方案评估.md`

- 文件内容与实际实现完全一致，无需修改

---

## 三、具体修改内容

### 3.1 StageB_核心流程跑通计划.md - B1 部分

**原内容**：
```
**影响文件：**
- 新建 `domain/Task.kt`
- 新建 `domain/TaskStep.kt`
- 新建 `domain/Category.kt`
- 新建 `domain/TaskType.kt`
- 新建 `domain/TaskStatus.kt`
- 新建 `domain/StepStatus.kt`
- 页面内分散模型定义保留至 `B5` 统一迁移

**验证标准：**
- 所有领域模型文件创建完成
- 字段类型符合 Long ID、Int 枚举、Long 时间戳约束
- `plannedStartAt` 为 `Long?`，用于表示可选的计划开始时间
- 无 Room 注解（属 Stage C）
```

**修改后**：
```
**影响文件：**
- 新建 `domain/Task.kt`
- 新建 `domain/TaskStep.kt`
- 新建 `domain/Category.kt`
- 新建 `domain/TaskType.kt`
- 新建 `domain/TaskStatus.kt`
- 新建 `domain/StepStatus.kt`
- 新建 `domain/PomodoroSession.kt`（用于番茄钟会话，Stage D 落库）
- 新建 `domain/SessionStatus.kt`（番茄钟会话状态枚举）
- 页面内分散模型定义保留至 `B5` 统一迁移

**验证标准：**
- 所有领域模型文件创建完成（共 8 个）
- 字段类型符合 Long ID、Int 枚举、Long 时间戳约束
- `plannedStartAt` 为 `Long?`，用于表示可选的计划开始时间
- `PomodoroSession` 的 `stepId` 为 `Long?`，用于可选关联步骤
- 无 Room 注解（属 Stage C）
```

### 3.2 StageB_核心流程跑通计划.md - B2 部分

**原内容**：
```
**影响文件：**
- 新建 `data/repository/TaskQuery.kt`（组合查询数据类）
- 新建 `data/repository/TaskRepository.kt`（接口）
- 新建 `data/repository/CategoryRepository.kt`（接口）
- 新建 `data/repository/PomodoroRepository.kt`（接口，Stage B 仅定义）
```

**修改后**：无需修改，已包含 PomodoroRepository

---

## 四、风险评估

| 风险 | 影响 | 应对策略 |
|------|------|---------|
| PomodoroSession 实体在 Stage B 创建，但其 Repository 实现延后至 Stage D | 实体已存在但无完整实现 | 这是合理的设计，实体定义先行，实现延后 |
| 文档与实际不一致可能导致后续开发困惑 | 低 | 更新文档后，后续开发以文档为准 |

---

## 五、实施步骤

1. 修改 `StageB_核心流程跑通计划.md` 中 B1 部分的影响文件和验证标准
2. 确认其他文档无需修改
3. 通知用户文档已更新