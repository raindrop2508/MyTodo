# 补充架构说明文档与 UI 骨架阶段分析

## Summary（任务概述）

基于对 `c:\code\MyTodo` 实际代码的探索，发现《项目技术架构与实现说明.md》与代码现状存在多处偏差（缺失包/文件、页面实现细节描述不准、未记录验收对照），同时《UI骨架验收清单.md》Stage A 存在 6 项未勾选 + 1 项已勾但有偏差，需对文档进行补充使其如实反映工程现状，并在对话中给出「能否进入下一阶段」的结论。

本任务**仅修改文档，不修改任何代码/资源**。

## Current State Analysis（现状分析）

### 文档与代码的偏差点（通过探索确认）

1. **项目结构缺失项**
   - 文档第 2.1 节包结构未包含 `ui/common/` 包（实际含 `AddTaskBottomSheetHelper.kt`、`EditableStep.kt`、`EditableStepAdapter.kt`）。
   - 未记录 `ui/tasks/TaskEditActivity.kt`（任务编辑页，实际已存在并在 AndroidManifest 注册）。
   - 未记录 layout 资源清单（实际 13 个布局）、menu 资源（`menu_task_detail.xml`、`menu_task_edit.xml`）、遗留资源（`strings_main.xml`、`dimens_main.xml`）。

2. **页面实现细节描述不准**
   - **Today**：文档表述为"四象限 Chip 分组（全部/紧急且重要/重要/紧急/其他）"，准确；但未说明未按四象限分组渲染（平铺 + 按 done 排序），且未实现"单次/长时"快速筛选。
   - **Tasks**：文档表述维度为"按类别/按象限/按状态"，准确；但未说明缺少"任务类型"与"日期范围"筛选控件（代码内定义了未使用的 `TypeFilter` 枚举）。
   - **Statistics**：文档表述时间为"日/月/年"，实际 UI 文案为"今日/本周/本月"但代码映射到 `DAY/MONTH/YEAR` 枚举（标注 Placeholder），存在文案与模式错位；类别筛选默认选中"全部类别"而非"仅长时"；**模块 C（类别步骤完成时间）容器缺失**，仅 A/B 存在；无"仅统计长时任务/步骤"口径说明与空态。
   - **Settings**：文档表述数据管理为"导出数据"，准确；但未说明缺少"导入"与"清空"入口。
   - **TaskDetail**：文档表述准确（单次任务隐藏番茄钟按钮 + 显示原因），但未说明启动 `PomodoroActivity` 时未传递任何任务参数。
   - **Pomodoro**：文档未说明计时硬编码 25 分钟未读取设置页配置，且无 `isLongTask` 兜底校验。

3. **数据层描述不完整**
   - 文档已说明"仅 AppInfo 表，未接入业务数据"，准确；但未明确各页面 mock 数据各自独立的现状（Today/Tasks/TaskDetail/TaskEdit/Statistics 五处分散），也无 Repository/FakeRepository。

### 验收清单 Stage A 状态（核对结论）

- **未勾选 6 项**（与实际一致）：
  - A2 快速筛选 全部/单次/长时 → 实际为四象限维度
  - A2 任务列表按四象限分组 → 平铺未分组
  - A3 筛选区 类别/任务类型/日期范围 → 缺任务类型与日期范围
  - A4 顶部筛选 日/周/月、类别、任务类型（默认仅长时） → 时间映射错位、默认全选、无独立任务类型
  - A4 模块 A 空态/说明"仅统计长时任务/步骤" → 无
  - A5 数据管理 导入/导出/清空 → 仅导出
  - A6 Statistics 模块 A 口径"单次任务不纳入统计" → 无
- **已勾但有偏差 1 项**：
  - A4「模块 A/B/C 容器结构存在」已勾，但模块 C 缺失，仅 A/B 存在。

## Proposed Changes（具体改动）

### 改动 1：补充 `docs/项目技术架构与实现说明.md`

**目标**：使文档与代码现状一致，并新增验收对照章节（不含阶段进入性结论，结论仅在对话中回复）。

**具体改动点**：

1. **第 2.1 节 包结构说明**
   - 在 `ui/` 包下补充 `common/` 子包及其 3 个文件（`AddTaskBottomSheetHelper.kt`、`EditableStep.kt`、`EditableStepAdapter.kt`）。
   - 在 `ui/tasks/` 下补充 `TaskEditActivity.kt`。
   - 注明 `ui/pomodoro/PomodoroActivity.kt` 已存在。

2. **新增第 2.3 节 资源文件清单**（在模块划分后）
   - layout：列出 13 个布局文件并归类（activity/fragment/item/bottom_sheet）。
   - menu：`bottom_nav_menu.xml`、`menu_task_detail.xml`、`menu_task_edit.xml`。
   - navigation：`main_nav_graph.xml`。
   - values：`strings.xml`、`strings_main.xml`（标注遗留）、`colors.xml`、`dimens.xml`、`dimens_main.xml`（遗留）、`styles.xml`、`themes.xml`、`values-night/themes.xml`。
   - drawable：简要列举关键自定义 drawable（标签背景、分段控制器、圆形复选框选择器）。

3. **第 4.2 节 Today 页面**
   - 在"核心功能"中补充说明：列表为平铺渲染（按 done 排序），未按四象限分组；未实现"单次/长时"快速筛选（与验收清单 A2 未勾项对应）。

4. **第 4.3 节 Tasks 页面**
   - 补充说明：维度为 类别/象限/状态，未实现"任务类型"与"日期范围"筛选控件（代码内 `TypeFilter` 枚举定义但未接入 UI）；列表项复用 `item_today_task.xml`，通过 `tvStepTag` 展示步骤数摘要。

5. **第 4.4 节 TaskDetail 页面**
   - 补充说明：新建/编辑通过 `TaskEditActivity`（独立 Activity）承载，FAB 弹窗与 TaskEdit 均为占位实现；启动 `PomodoroActivity` 时未传递 taskId/title 等参数。

6. **新增第 4.x 节 TaskEdit 页面**（在 Task Detail 后、Statistics 前）
   - 说明：独立 Activity，表单含名称/类别/紧急/重要/任务类型/计划时间等字段；步骤编辑通过 `EditableStepAdapter`；为占位实现，未接 Repository。

7. **第 4.5 节 Statistics 页面**
   - 修正时间维度描述：UI 文案为"今日/本周/本月"，代码映射到 `DAY/MONTH/YEAR` 枚举（标注 Placeholder），存在文案与模式错位。
   - 补充说明：类别筛选为"全部类别/仅长时任务"两档，默认选"全部类别"（非"仅长时"）；无独立"任务类型"筛选控件。
   - 补充说明：模块结构实际仅模块 A（图表卡）与模块 B（概览卡片），**模块 C（类别步骤完成时间）容器缺失**。
   - 补充说明：无"仅统计长时任务/步骤"口径说明，无空态视图。

8. **第 4.6 节 Settings 页面**
   - 补充说明：数据管理仅有"导出数据"入口（Toast 占位），缺少"导入"与"清空"入口。

9. **第 4.7 节 Pomodoro 页面**
   - 补充说明：计时硬编码 25 分钟，未读取设置页 `focusMinutes`；无 `isLongTask` 兜底校验（异常路径风险）。

10. **第 5.3 节 当前数据来源（Mock 数据）**
    - 扩展表格：补充 TaskEdit（`buildMockSteps()`）与 Statistics 现状行；强调五处 mock 各自独立、无统一 Repository。

11. **新增第 9 章 阶段验收对照**（追加在文末附录前）
    - 对照表：列出验收清单 Stage A 全部项的勾选状态、实际实现、是否一致。
    - 标注已勾偏差项（A4 模块 A/B/C）。
    - 列出 6 项未完成项的简述。
    - **不写「能否进入下一阶段」结论**（按用户要求仅在对话中回复）。

12. **第 8.1 节 已知问题表**
    - 补充遗漏项：Statistics 时间映射错位（今日/本周/本月 → DAY/MONTH/YEAR）、模块 C 缺失、Pomodoro 未读取设置时长、TaskDetail 启动 Pomodoro 未传参。

### 改动 2：补充 `docs/ui_skeleton/UI骨架验收清单.md` 偏差备注

**目标**：对「已勾但有偏差」的项追加实际偏差备注，保留勾选状态不变；对未勾项补充简要现状说明，提升清单参考价值。

**具体改动点**：

1. **A4「模块 A/B/C 的容器结构存在（可以是卡片占位）」**（已勾）
   - 在该行后追加备注：`> 实际偏差：仅模块 A（时间段分布图表卡）与模块 B（完成情况概览卡片）存在，模块 C（类别步骤完成时间）容器缺失。`

2. **未勾项追加现状说明**（每项后加 `— 实际：…` 简述，不改勾选状态）：
   - A2 快速筛选：`— 实际：维度为四象限（全部/紧急且重要/重要/紧急/其他），无单次/长时筛选`
   - A2 四象限分组：`— 实际：列表平铺渲染（按完成状态排序），未按象限分组`
   - A3 筛选区控件：`— 实际：维度为 类别/象限/状态，缺任务类型与日期范围`
   - A4 顶部筛选：`— 实际：时间文案为今日/本周/本月但映射到 DAY/MONTH/YEAR；类别默认选"全部"；无独立任务类型筛选`
   - A4 模块 A 空态/说明：`— 实际：无口径说明文案，无空态视图`
   - A5 数据管理：`— 实际：仅有"导出数据"入口，缺导入与清空`
   - A6 Statistics 口径：`— 实际：无"单次任务不纳入统计"口径表达`

3. **不修改 `UI骨架实现方案.md`**：该文档为实施说明（方案性文档），无勾选项，保持原样。

## Assumptions & Decisions（假设与决策）

1. **不修改任何代码/资源**：本任务为文档补充，所有偏差仅记录于文档与对话中。
2. **验收清单勾选状态保持不变**：用户明确要求「保留勾选加偏差备注」，仅追加说明，不改 `[x]`/`[ ]`。
3. **阶段进入性结论仅在对话中回复**：不写入架构说明文档，文档中新增的第 9 章仅做客观对照，不下结论。
4. **`UI骨架实现方案.md` 不改动**：该文档为前置方案说明，与验收无关，且用户未要求修改。
5. **文档语言**：保持中文，与现有文档一致；代码引用使用 `file:///` 协议可点击链接。
6. **探索结果可信**：以搜索代理对实际代码的核对结果为准，文档描述据此校正。

## Verification Steps（验证步骤）

1. **一致性校验**：补充后逐项比对文档描述与实际代码（包结构、页面实现细节、数据来源表）。
2. **验收清单核对**：确认 7 处备注（1 处已勾偏差 + 6 处未勾现状）与探索结果一致，勾选符号未被改动。
3. **链接有效性**：架构说明文档中新增的 `file:///` 链接指向真实存在的文件路径（基于 `c:\code\MyTodo` 实际结构，注意原文档使用 `e:/code/MyPotato` 旧路径，本次补充沿用既有路径风格以保持一致，不批量校正旧链接——超出本任务范围）。
4. **不越界**：确认未修改 `UI骨架实现方案.md`、未修改任何代码、未在文档中写入阶段结论。
5. **对话结论**：最终回复中给出明确的「能否进入 Stage B」结论 + 阻塞项清单 + 建议。
