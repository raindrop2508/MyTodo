# MyPotato 项目技术架构与实现说明

> 状态：有效  
> 最后更新：2026-09-05  
> 适用范围：项目架构与实现的阶段性总览（与当前代码对齐）  
> 权威性：实现现状以本文件 + 实际代码为准；阶段细节见 `docs/plan/stage*/`

---

## 1. 项目概述

### 1.1 项目定位

MyPotato 是一款基于 Android 原生开发的番茄钟 TODO 应用，融合四象限优先级管理与番茄工作法，帮助用户聚焦重要任务。

**当前实现口径（2026-09）：**

- Stage A–C 已完成：UI 骨架 → Repository/ViewModel 主链路 → Room 业务持久化
- Stage D 进行中：番茄钟会话落库（D1）已完成；统计三模块仍待接真数据
- 活动计时持久化已落地（DB v3）：运行中/暂停中会话字段落库 + 冷启动孤儿收尾
- 运行时仅使用 `Room*Repository`；`Fake*Repository` 已从工程移除

### 1.2 技术栈总览

| 技术领域 | 选型 | 说明 |
|---------|------|------|
| 开发语言 | Kotlin | 100% Kotlin |
| UI 框架 | ViewBinding + XML + Material 3 | `viewBinding` / `dataBinding` 均已开启 |
| 导航 | Jetpack Navigation Component | 底部 4 入口；深层页为独立 Activity |
| 架构模式 | 单 Activity + 多 Fragment + MVVM | 页面级 ViewModel + `ViewModelFactory` |
| 数据持久化 | Room 2.x | DB `potato_db` **version = 3**，5 张表 |
| 响应式状态 | Coroutines + Flow / StateFlow | DAO Flow → Repository → ViewModel → UI |
| 构建工具 | Gradle + Version Catalog | `gradle/libs.versions.toml` |
| 最低 SDK | API 29 | minSdk = 29 |
| 目标/编译 SDK | API 36 | targetSdk = compileSdk = 36 |
| 图表 | 自定义 `StatisticsStackBarView` | Stage D 计划评估 MPAndroidChart，尚未引入 |

---

## 2. 项目结构

### 2.1 包结构说明

```
app/src/main/java/com/gordon/mypotato/
├── MainActivity.kt                 # Shell：底部导航 + 冷启动孤儿会话弹窗
├── MyPotatoApp.kt                  # Application：DB 单例入口 + 默认数据初始化
├── data/
│   ├── AppDatabase.kt              # Room 数据库（v3）
│   ├── dao/                        # AppInfo / Task / TaskStep / Category / PomodoroSession
│   ├── entity/                     # 对应 Entity
│   ├── mapper/EntityMapper.kt      # Entity ↔ Domain
│   ├── initializer/DatabaseInitializer.kt
│   └── repository/                 # 接口 + Room 实现（无 Fake）
├── domain/                         # 纯领域模型与计时逻辑
│   ├── Task / TaskStep / Category / PomodoroSession
│   ├── TaskType / TaskStatus / StepStatus / SessionStatus / PomodoroPhase
│   ├── PomodoroTimerLogic.kt       # 阶段流转与专注时长计算
│   └── OrphanPomodoroSettlement.kt # 冷启动收尾协调
├── viewmodel/                      # 页面 ViewModel + ViewModelFactory
└── ui/
    ├── common/                     # BottomSheet / 步骤编辑 / CategoryChipHelper
    ├── today/ / tasks/ / statistics/ / settings/ / pomodoro/
```

### 2.2 模块划分

单模块工程（`app`），按职责分层：

| 层 | 包 | 职责 |
|----|-----|------|
| UI | `ui/` | Fragment / Activity / 自定义 View / Adapter |
| ViewModel | `viewmodel/` | 状态持有、写操作、规则校验 |
| Domain | `domain/` | 业务模型与纯逻辑（可单测） |
| Data | `data/` | Room Entity/DAO/Repository/Mapper/Initializer |

### 2.3 资源文件清单

**布局（`res/layout/`，13 个）：**

| 类别 | 文件 |
|------|------|
| Activity | `activity_main`、`activity_task_detail`、`activity_task_edit`、`activity_pomodoro` |
| Fragment | `fragment_today`、`fragment_tasks`、`fragment_statistics`、`fragment_settings` |
| 列表项 | `item_today_task`、`item_step`、`item_step_editable`、`item_statistics_timeline_slot` |
| BottomSheet | `bottom_sheet_add_task_placeholder` |

**菜单：** `bottom_nav_menu.xml`、`menu_task_detail.xml`、`menu_task_edit.xml`  
**导航：** `main_nav_graph.xml`（4 个底部目的地；深层页未纳入）  
**主题：** `values/themes.xml` + `values-night/themes.xml`（深色占位；MainActivity 仍强制浅色）

---

## 3. 架构设计

### 3.1 整体架构

```
UI (Fragment/Activity)
        │
        ▼
ViewModel (StateFlow)
        │
        ▼
Repository 接口
        │
        ▼
Room*Repository → DAO (Flow) → potato_db (v3)
```

- `MainActivity`：底部导航容器；冷启动时通过 `OrphanPomodoroSettlement` 处理残留活动会话
- 一级页：Today / Tasks / Statistics / Settings（Fragment）
- 深层页：`TaskDetailActivity` / `TaskEditActivity` / `PomodoroActivity`（Intent 传参）

### 3.2 导航

```
MainActivity
├── Today (startDestination)
├── Tasks
├── Statistics
└── Settings

独立 Activity：
├── TaskDetailActivity  ← taskId
├── TaskEditActivity    ← taskId（新建/编辑）
└── PomodoroActivity    ← taskId（+ 可选 stepId）
```

参数传递：Fragment 间可用 Safe Args；深层 Activity 以 Intent extras 为主，风格并存。

### 3.3 数据层

**数据库：** `AppDatabase`，名 `potato_db`，**version = 3**，开发期 `fallbackToDestructiveMigration`。

| 表 | Entity | 要点 |
|----|--------|------|
| `app_info` | `AppInfo` | 库元信息 |
| `task` | `TaskEntity` | 四象限、类型、状态、外键 Category |
| `task_step` | `TaskStepEntity` | 级联删除 Task |
| `category` | `CategoryEntity` | 名称唯一索引 |
| `pomodoro_session` | `PomodoroSessionEntity` | 关联 Task/Step；含活动计时字段（见下） |

**`pomodoro_session` 活动计时字段（DB v3 新增）：**

| 列名 | 含义 |
|------|------|
| `phase` | 计时阶段：`0` FOCUS / `1` SHORT_BREAK / `2` LONG_BREAK |
| `planned_duration_ms` | 本阶段计划时长（毫秒） |
| `target_end_epoch_ms` | 运行中墙钟目标结束时刻；暂停/终态为 `NULL` |
| `remaining_ms_when_paused` | 暂停时剩余倒计时（毫秒） |
| `pause_started_at_epoch_ms` | 本次暂停开始墙钟时刻；未暂停为 `0` |

权威字段说明（写入时机、与 `focus_duration_sec` / `paused_duration_sec` 衔接）：[StageD_番茄钟计时持久化_实际落地方案](plan/stageD/StageD_番茄钟计时持久化_实际落地方案.md) §2.1.1。

**会话状态：** `SessionStatus` = `IN_PROGRESS(0)` / `COMPLETED(1)` / `INTERRUPTED(2)` / `PAUSED(3)`  
**阶段：** `PomodoroPhase` = `FOCUS` / `SHORT_BREAK` / `LONG_BREAK`

**默认数据：** `DatabaseInitializer` 在 `MyPotatoApp` 启动时写入默认分类与演示任务（若库为空）。

**依赖注入方式：** `ViewModelFactory.getInstance(context)` 内创建 `RoomTaskRepository` / `RoomCategoryRepository` / `RoomPomodoroRepository`（无 DI 框架）。

---

## 4. 核心页面实现（当前）

> 以下描述对齐当前代码，不再沿用 Stage A 的内联 Mock 口径。

### 4.1 MainActivity

- 绑定 `BottomNavigationView` ↔ `NavController`
- `AppCompatDelegate.MODE_NIGHT_NO` 强制浅色（待 Stage E 移除）
- `savedInstanceState == null` 时检查孤儿番茄钟会话并弹窗收尾

### 4.2 Today

- `TodayViewModel` + `TaskRepository` / `CategoryRepository`
- 四象限 Chip 筛选、列表勾选完成、FAB → `AddTaskBottomSheetHelper` 新建并落库
- 点击任务跳转 `TaskDetailActivity`

### 4.3 Tasks

- `TasksViewModel`：搜索 + 类别/象限/状态维度筛选
- 与 Today 共用统一数据源与列表项布局
- FAB 新建任务同链路落库

### 4.4 TaskDetail / TaskEdit

- `TaskDetailViewModel`：按 `taskId` 观察任务与步骤；长时任务可进番茄钟；单次任务隐藏入口并说明原因
- `TaskEditViewModel`：编辑标题/类型/分类/步骤等并写回 Repository；分类 Chip 由 `CategoryChipHelper` 动态生成

### 4.5 Pomodoro

- `PomodoroViewModel`：读取设置页时长；阶段机；开始/暂停/继续/重置/完成均落库
- 全库活动会话 ≤ 1；开新会话前打断其它活动行
- 专注时长按墙钟减暂停计算；仅专注完成增加 cycle；休息结束后回到 `FOCUS`
- 非长时任务：`isValidTask = false`，拒绝计时

### 4.6 Statistics（未闭环）

- 仍使用 Fragment 内 Mock 数据 + `StatisticsStackBarView`
- 时间 Chip「今日/本周/本月」映射到 `DAY/MONTH/YEAR` 仍错位
- 模块 C（类别步骤完成时间）容器缺失
- **下一步：** Stage D 的 D2–D8（图表选型、三模块真数据、口径说明、空态、时间维度修复）

### 4.7 Settings

- `SettingsViewModel` + SharedPreferences：专注/短休/长休/长休间隔、提醒开关等
- 主题/语言/导入导出多为占位；主题因 MainActivity 强制浅色暂不生效

---

## 5. 数据层与番茄钟闭环细节

### 5.1 Room 配置

- 依赖：`room-runtime` + `room-ktx` + `room-compiler`（KSP）
- 单例：双重检查锁 + `applicationContext`

### 5.2 Repository 接口边界

| 接口 | 职责概要 |
|------|----------|
| `TaskRepository` | 任务/步骤 CRUD、筛选、状态流转 |
| `CategoryRepository` | 分类 CRUD |
| `PomodoroRepository` | 会话 CRUD、活动会话查询、中断/完成 |

### 5.3 番茄钟落库与冷启动（已实现）

详见 [番茄钟活动计时持久化](plan/stageE/番茄钟活动计时持久化.md)。

| 能力 | 状态 |
|------|------|
| 会话历史落库（D1） | ✅ |
| 活动字段持久化 | ✅ |
| 冷启动用户确认收尾 | ✅ |
| 杀进程后静默恢复倒计时 | ❌ 明确不做 |
| 正式 Migration（非 destructive） | ❌ 发布前再补 |

**统计口径（已定，待统计页接入）：** 仅 `phase == FOCUS` 且 `status == COMPLETED`。

### 5.4 Stage D 剩余项

| 步骤 | 内容 | 状态 |
|------|------|------|
| D1 | 番茄钟会话落库 | ✅ |
| D2 | 图表库选型与集成 | 待做 |
| D3–D5 | 统计模块 A/B/C | 待做 |
| D6 | 口径说明与空态 | 待做 |
| D7 | 时间维度映射修复 | 待做 |
| D8 | 验证与测试 | 待做 |

计划全文：[StageD_番茄钟会话落库与统计闭环计划](plan/stageD/StageD_番茄钟会话落库与统计闭环计划.md)

---

## 6. UI 设计规范

### 6.1 Material 3 主题

- 基类链路：`Theme.MyPotato` → Material3 DayNight / Light NoActionBar（以 `themes.xml` 为准）
- 主色：`brand_indigo` (#4F46E5) 等，见 `colors.xml`
- 颜色优先走 `?attr/colorXxx`；矢量图占位黑 + `app:tint`

### 6.2 分类标签色

工作 / 购物·生活 / 学习 / 健康 / 默认 各自有背景色与文字色（`colors.xml`）。

### 6.3 自定义组件

- `StatisticsStackBarView`：Canvas 堆叠柱状图
- 标签 / 分段控制器 / 圆形复选框 / Chip 选择器等 Drawable

---

## 7. 核心技术点

### 7.1 ViewBinding

Fragment 使用 `_binding` 可空 + `binding` 非空，`onDestroyView()` 置空。

### 7.2 RecyclerView

内部 Adapter + ViewBinding；多数仍 `notifyDataSetChanged()`，未上 DiffUtil/ListAdapter。

### 7.3 领域纯逻辑单测

- `PomodoroTimerLogicTest`、`OrphanPomodoroSettlementTest` 覆盖阶段流转与收尾
- UI / Repository 集成测试仍薄弱；`DatabaseCreationTest` 覆盖建库

### 7.4 BottomSheet 新建任务

`AddTaskBottomSheetHelper` 统一封装，Today/Tasks 复用同一布局并回调 ViewModel 写库。

---

## 8. 当前局限与后续规划

### 8.1 已知问题

| 问题 | 优先级 | 状态 |
|------|--------|------|
| MainActivity 强制浅色 | 中 | 待修复 |
| Statistics 仍为 Mock + 时间映射错位 | 高 | Stage D 待做 |
| Statistics 模块 C 缺失 | 高 | Stage D 待做 |
| Settings 缺导入/清空 | 中 | 待补 |
| Today 筛选与早期验收清单「单次/长时」不一致 | 中 | 产品取舍待定 |
| Safe Args 与 Intent 并存 | 中 | 可后续统一 |
| RecyclerView 全量刷新 | 低 | 可优化 |
| 无正式 Room Migration | 中 | 发布前补 |

### 8.2 技术债

| 项 | 状态 |
|----|------|
| Repository / ViewModel / 统一领域模型 | ✅ |
| Room 业务持久化 | ✅ |
| FakeRepository 残留 | ✅ 已移除 |
| 番茄钟活动计时 + 孤儿收尾 | ✅ |
| 深色模式正式上线 | 待做 |
| i18n 全量落地 | 待做 |
| 统计真数据闭环 | 待做 |
| 测试覆盖扩大 | 部分单测已有，整体待补 |

### 8.3 阶段完成摘要

- **Stage B：** 接口 + ViewModel + 主流程闭环（历史 Fake 实现）
- **Stage C：** Entity/DAO/RoomRepository/Initializer/切换工厂
- **Stage D（部分）：** D1 会话落库 + 活动计时持久化；统计未闭环
- **Stage E：** 规划中（深色、i18n、质量加固）；活动计时专题文档放在 `plan/stageE/`

---

## 9. 阶段验收对照（Stage A，历史）

本章保留 Stage A 验收对照，用于追溯 UI 骨架缺口，**不代表当前业务完成度**。

- Stage A 共 21 子项：已勾选 14（含 A4.2 模块 C 偏差），未勾选 7
- 未完成项集中：Today 单次/长时筛选与四象限分组、Tasks 类型/日期筛选、Statistics 口径与模块 C、Settings 导入/清空
- 详细对照表见历史版本或 `docs/plan/stageA/ui_skeleton/UI骨架验收清单.md`

---

## 附录：核心文件索引

| 文件 | 路径 |
|------|------|
| Application | `app/src/main/java/com/gordon/mypotato/MyPotatoApp.kt` |
| 主 Activity | `app/src/main/java/com/gordon/mypotato/MainActivity.kt` |
| Room DB | `app/src/main/java/com/gordon/mypotato/data/AppDatabase.kt` |
| ViewModel 工厂 | `app/src/main/java/com/gordon/mypotato/viewmodel/ViewModelFactory.kt` |
| 番茄钟 VM | `app/src/main/java/com/gordon/mypotato/viewmodel/PomodoroViewModel.kt` |
| 计时逻辑 | `app/src/main/java/com/gordon/mypotato/domain/PomodoroTimerLogic.kt` |
| 孤儿收尾 | `app/src/main/java/com/gordon/mypotato/domain/OrphanPomodoroSettlement.kt` |
| 主题 / 颜色 / 字符串 | `app/src/main/res/values/themes.xml` 等 |
| 导航图 | `app/src/main/res/navigation/main_nav_graph.xml` |
| 依赖 Catalog | `gradle/libs.versions.toml` |
| 文档索引 | `docs/README.md` |
| Agent 约定 | `agent.md` |
