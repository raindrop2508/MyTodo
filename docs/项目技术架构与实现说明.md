# MyPotato 项目技术架构与实现说明

> 状态：有效（含 Stage A 历史背景）
> 最后更新：2026-07-10
> 适用范围：项目架构与实现的阶段性总览
> 权威性：当前实现现状请优先结合 `docs/stageB/StageB_整体实现总结.md` 一并阅读

## 1. 项目概述

### 1.1 项目定位

MyPotato 是一款基于 Android 原生开发的番茄钟 TODO 应用，融合了四象限优先级管理与番茄工作法，帮助用户聚焦重要任务。本文保留了 Stage A 的页面实现背景，同时补充了 Stage B 之后的当前实现口径：项目已完成 UI 骨架，并已接入 `Repository + RoomRepository + ViewModel + Flow/StateFlow` 主数据链路，数据已持久化到 Room 数据库。

### 1.2 技术栈总览

| 技术领域 | 选型 | 说明 |
|---------|------|------|
| 开发语言 | Kotlin | 100% Kotlin 实现 |
| UI 框架 | ViewBinding + XML 布局 + Material 3 | 视图绑定启用，dataBinding 备用 |
| 导航 | Jetpack Navigation Component | 底部导航 + Fragment 切换 |
| 架构模式 | 单 Activity + 多 Fragment + MVVM | 页面级 ViewModel 已接入 |
| 数据持久化 | Room | 已集成，5 张表（app_info, task, task_step, category, pomodoro_session） |
| 响应式状态 | Kotlin Coroutines + Flow/StateFlow | Stage B 已用于主数据链路 |
| 构建工具 | Gradle + Version Catalog | libs.versions.toml 统一管理依赖版本 |
| 最低 SDK | API 29 (Android 10) | minSdk = 29 |
| 目标 SDK | API 36 | targetSdk = 36 |
| 编译 SDK | API 36 | compileSdk = 36 |

---

## 2. 项目结构

### 2.1 包结构说明

```
app/src/main/java/com/gordon/mypotato/
├── MainActivity.kt              # 主 Activity，承载底部导航与 Fragment 容器
├── MainViewModel.kt             # 共享 ViewModel（目前仅初始化数据库）
├── data/                        # 数据层
│   ├── AppDatabase.kt           # Room 数据库单例
│   ├── dao/
│   │   ├── AppInfoDao.kt        # AppInfo DAO 接口
│   │   ├── TaskDao.kt           # 任务 DAO 接口
│   │   ├── TaskStepDao.kt       # 步骤 DAO 接口
│   │   ├── CategoryDao.kt       # 分类 DAO 接口
│   │   └── PomodoroSessionDao.kt # 番茄钟会话 DAO 接口
│   ├── entity/
│   │   ├── AppInfo.kt           # 应用信息实体类
│   │   ├── TaskEntity.kt        # 任务实体类
│   │   ├── TaskStepEntity.kt    # 步骤实体类
│   │   ├── CategoryEntity.kt    # 分类实体类
│   │   └── PomodoroSessionEntity.kt # 番茄钟会话实体类
│   ├── mapper/
│   │   └── EntityMapper.kt      # Entity-Domain 双向映射工具
│   ├── initializer/
│   │   └── DatabaseInitializer.kt # 默认数据初始化工具
│   └── repository/              # 仓储层
│       ├── TaskRepository.kt    # 任务仓储接口
│       ├── CategoryRepository.kt # 分类仓储接口
│       ├── PomodoroRepository.kt # 番茄钟仓储接口
│       ├── TaskQuery.kt         # 组合查询条件
│       ├── FakeTaskRepository.kt # 任务仓储内存实现
│       ├── FakeCategoryRepository.kt # 分类仓储内存实现
│       ├── FakePomodoroRepository.kt # 番茄钟仓储内存实现
│       ├── RoomTaskRepository.kt # 任务仓储 Room 实现
│       ├── RoomCategoryRepository.kt # 分类仓储 Room 实现
│       └── RoomPomodoroRepository.kt # 番茄钟仓储 Room 实现
├── domain/                      # 领域模型层
│   ├── Task.kt                  # 任务领域模型
│   ├── TaskStep.kt              # 步骤领域模型
│   ├── Category.kt              # 分类领域模型
│   ├── PomodoroSession.kt       # 番茄钟会话领域模型
│   ├── TaskType.kt              # 任务类型枚举
│   ├── TaskStatus.kt            # 任务状态枚举
│   ├── StepStatus.kt            # 步骤状态枚举
│   └── SessionStatus.kt         # 番茄钟会话状态枚举
├── viewmodel/                   # 视图模型层
│   ├── ViewModelFactory.kt      # ViewModel 工厂
│   ├── BaseTaskViewModel.kt     # 任务基类 ViewModel
│   ├── TodayViewModel.kt        # Today 页面 ViewModel
│   ├── TasksViewModel.kt        # Tasks 页面 ViewModel
│   ├── TaskDetailViewModel.kt   # 任务详情 ViewModel
│   ├── TaskEditViewModel.kt     # 任务编辑 ViewModel
│   ├── PomodoroViewModel.kt     # 番茄钟 ViewModel
│   └── SettingsViewModel.kt     # 设置页面 ViewModel
└── ui/                          # UI 层
    ├── common/                   # 公共组件
    │   ├── AddTaskBottomSheetHelper.kt # 新建任务底部弹窗封装
    │   ├── EditableStep.kt       # 步骤编辑数据模型
    │   └── EditableStepAdapter.kt # 步骤编辑 RecyclerView 适配器
    ├── today/
    │   └── TodayFragment.kt     # Today 首页
    ├── tasks/
    │   ├── TasksFragment.kt     # Tasks 任务列表页
    │   ├── TaskDetailActivity.kt # 任务详情页
    │   └── TaskEditActivity.kt  # 任务编辑页（新建/编辑）
    ├── statistics/
    │   ├── StatisticsFragment.kt # 统计页
    │   └── StatisticsStackBarView.kt # 堆叠柱状图自定义 View
    ├── settings/
    │   └── SettingsFragment.kt  # 设置页
    └── pomodoro/
        └── PomodoroActivity.kt  # 番茄钟页面
```

### 2.2 模块划分

当前为单模块工程（app module），按职责分层：

- **UI 层**（`ui/` 包）：负责页面展示与用户交互，包含 Fragment、Activity、自定义 View
- **数据层**（`data/` 包）：负责数据持久化，包含 Room 数据库、Entity、DAO
- **架构组件**（根包）：MainActivity 作为 shell，MainViewModel 作为共享状态容器

### 2.3 资源文件清单

**布局（layout/，共 13 个）：**

| 类别 | 文件 |
|------|------|
| Activity | `activity_main.xml`、`activity_task_detail.xml`、`activity_task_edit.xml`、`activity_pomodoro.xml` |
| Fragment | `fragment_today.xml`、`fragment_tasks.xml`、`fragment_statistics.xml`、`fragment_settings.xml` |
| 列表项 | `item_today_task.xml`、`item_step.xml`、`item_step_editable.xml`、`item_statistics_timeline_slot.xml` |
| BottomSheet | `bottom_sheet_add_task_placeholder.xml` |

**菜单（menu/）：** `bottom_nav_menu.xml`（底部导航）、`menu_task_detail.xml`（详情页编辑/删除）、`menu_task_edit.xml`（编辑页菜单）

**导航（navigation/）：** `main_nav_graph.xml`（仅含 4 个底部目的地，深层页面未纳入导航图）

**值资源（values/）：**
- `strings.xml`：主字符串资源（页面文案、按钮、空态等）
- `strings_main.xml`：ViewModel 测试遗留的 3 条字符串（待清理）
- `colors.xml`：品牌色、分类标签色、状态色
- `dimens.xml` / `dimens_main.xml`（遗留）：尺寸定义
- `styles.xml`：自定义组件样式（如圆形复选框）
- `themes.xml`：Material3 主题配置
- `values-night/themes.xml`：深色主题占位（因 MainActivity 强制浅色，当前不生效）

**Drawable：** 标签背景（`bg_tag_rounded.xml` / `bg_tag_outline.xml` / `bg_tag_type.xml`）、分段控制器（`bg_segmented_container.xml` / `bg_segmented_item_checked.xml`）、圆形复选框（`sel_checkbox_circle.xml`）、Chip 颜色选择器（`chip_bg_color_selector.xml` / `chip_stroke_color_selector.xml`）等

---

## 3. 架构设计

> 说明：本章中的“当前状态”已按 Stage B 后现状更新；后续页面级实现细节若仍描述 Stage A 期间的 Mock 或占位方式，应理解为历史实现背景，而非当前唯一事实。

### 3.1 整体架构：单 Activity + 多 Fragment

采用 Google 推荐的单 Activity 架构模式：

- [MainActivity](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/MainActivity.kt) 作为应用的唯一入口 Activity
- 四个主要页面（Today / Tasks / Statistics / Settings）均以 Fragment 形式实现
- 通过 Jetpack Navigation Component 管理 Fragment 切换
- 深层页面（Task Detail / Pomodoro）以独立 Activity 形式存在，通过 Intent 跳转

**优点：**
- 职责清晰，Activity 专注于承载 Fragment 和底部导航
- Fragment 复用性好，生命周期管理更灵活
- Navigation 组件提供一致的导航体验

**局限：**
- Task Detail 和 Pomodoro 目前是独立 Activity，导航与参数协议仍有进一步统一空间
- Room 业务实体已落地，当前业务数据以 RoomRepository 为主，FakeRepository 保留作为历史实现

### 3.2 导航架构

**底部导航配置：**
- 菜单资源：[bottom_nav_menu.xml](file:///c:/code/MyTodo/app/src/main/res/menu/bottom_nav_menu.xml)
- 导航图：[main_nav_graph.xml](file:///c:/code/MyTodo/app/src/main/res/navigation/main_nav_graph.xml)
- 关联方式：`BottomNavigationView.setupWithNavController(navController)`

**导航结构：**
```
MainActivity (底部导航容器)
├── Today (startDestination)
├── Tasks
├── Statistics
└── Settings

独立 Activity：
├── TaskDetailActivity (从 Today/Tasks 跳转)
└── PomodoroActivity (从 TaskDetail 跳转)
```

**参数传递：**
- Fragment 间已部分引入 Navigation Safe Args
- 部分深层页面仍通过 Intent extras 传递 taskId、taskTitle 等字段，当前属于并存阶段

### 3.3 数据层架构

**Room 数据库配置：**
- 数据库类：[AppDatabase.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/data/AppDatabase.kt)
- 数据库名：`potato_db`
- 版本：1
- 迁移策略：`fallbackToDestructiveMigration`（开发阶段允许破坏性迁移）

**当前实体：**
- [AppInfo](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/data/entity/AppInfo.kt)：应用信息表，包含 dbVersion 和 createTime
- [TaskEntity](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/data/entity/TaskEntity.kt)：任务实体，外键关联 Category，支持四象限属性
- [TaskStepEntity](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/data/entity/TaskStepEntity.kt)：步骤实体，外键级联删除 Task
- [CategoryEntity](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/data/entity/CategoryEntity.kt)：分类实体，名称唯一索引
- [PomodoroSessionEntity](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/data/entity/PomodoroSessionEntity.kt)：番茄钟会话实体，外键关联 Task 和 TaskStep

**当前业务数据状态：**
- Repository 接口已建立
- RoomRepository 已作为当前业务数据实现落地（运行时已切换使用）
- FakeRepository 作为历史实现保留（可随时切换回测）
- ViewModel 已通过 `Flow/StateFlow` 与统一数据源联动
- Entity-Domain 双向映射工具已完成（[EntityMapper.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/data/mapper/EntityMapper.kt)）
- 默认数据初始化工具已完成（[DatabaseInitializer.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/data/initializer/DatabaseInitializer.kt)）
- 应用启动时自动执行默认数据初始化（通过 [MyPotatoApp.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/MyPotatoApp.kt)）

**Stage C 完成情况：**
- ✅ 步骤 C1：4 个 Room Entity 实体类创建完成
- ✅ 步骤 C2：4 个 DAO 接口创建完成
- ✅ 步骤 C3：AppDatabase 注册新表（5 张表），版本号保持为 1（开发阶段）
- ✅ 步骤 C4：Entity-Domain 映射工具创建完成
- ✅ 步骤 C5：3 个 RoomRepository 实现完成
- ✅ 步骤 C6：数据初始化工具创建完成
- ✅ 步骤 C7：ViewModelFactory 切换为 RoomRepository（已完成）
- ✅ 步骤 C8：验证与测试（已完成，项目编译通过）

**新增文件：**
- `MyPotatoApp.kt`：自定义 Application 类，管理数据库单例与默认数据初始化
- `AndroidManifest.xml`：注册自定义 Application

**后续规划方向：**
- 安装到设备进行运行验证
- 测试数据持久化、响应式刷新、默认数据初始化等功能

---

## 4. 核心页面实现

> 说明：本章大量页面描述来自 Stage A/Stage B 演进过程。若与当前代码实现或 `StageB_整体实现总结` 存在差异，应以后者和实际代码为准。

### 4.1 MainActivity：底部导航 + Fragment 容器

**文件：** [MainActivity.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/MainActivity.kt)

**核心职责：**
1. 设置内容视图（activity_main.xml）
2. 将 BottomNavigationView 与 NavController 绑定
3. （当前）强制应用为浅色模式

**关键代码点：**
- `setupWithNavController()`：一行代码关联底部导航与导航图
- `AppCompatDelegate.setDefaultNightMode(MODE_NIGHT_NO)`：临时强制浅色模式，深色模式适配完成后可移除

### 4.2 Today 页面

**文件：** [TodayFragment.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/today/TodayFragment.kt)

**页面结构：**
- 顶部：日期显示 + 问候语区域
- 筛选区：四象限 Chip 分组（全部 / 紧急且重要 / 重要 / 紧急 / 其他）
- 列表区：任务列表（RecyclerView）
- 悬浮按钮：FAB 新建任务
- 空态：无任务时显示引导

**核心功能：**
- 日期格式化显示（中文星期）
- 四象限筛选切换
- 任务列表展示（含分类标签、类型标签、时长、步骤数）
- 任务勾选完成（本地状态切换，完成任务沉底）
- 点击任务跳转详情页
- FAB 点击弹出 BottomSheet 新建任务表单

**数据来源：**
- `buildMockTasks()` 方法内联 Mock 数据（5 条示例任务）
- 数据以 `TodayTask` 内部数据类承载

**适配器模式：**
- `TodayTaskAdapter` 内部类继承 RecyclerView.Adapter
- 通过构造函数注入点击回调和勾选回调
- 使用 ViewBinding 绑定 item_today_task.xml

**实现现状备注：**
- 当前筛选维度为四象限（全部 / 紧急且重要 / 重要 / 紧急 / 其他），**未实现验收清单 A2 要求的"全部 / 单次 / 长时"快速筛选**
- 任务列表为**平铺渲染**（按 `done` 字段排序，已完成沉底），**未按四象限分组展示**（与 A2 未勾项对应）
- FAB 弹窗通过 `AddTaskBottomSheetHelper` 封装，回调仅打日志，未落库

### 4.3 Tasks 页面

**文件：** [TasksFragment.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/tasks/TasksFragment.kt)

**页面结构：**
- 顶部：标题 + 搜索框
- 维度切换：分段控制器（按类别 / 按象限 / 按状态）
- 筛选 Chip 区：对应三个维度的 Chip 分组
- 列表区：任务列表（RecyclerView，复用 Today 的 item 布局）
- 悬浮按钮：FAB 新建任务
- 空态：无任务时显示引导

**核心功能：**
- 搜索框实时模糊过滤（按标题）
- 三维度切换显示对应 Chip 组
- 多条件组合筛选（搜索关键字 + 当前维度筛选条件）
- 任务勾选完成
- 点击任务跳转详情页
- FAB 新建任务（复用 Today 的 BottomSheet 布局）

**技术亮点：**
- 维度切换时动态显示/隐藏对应 ChipGroup
- 筛选逻辑统一在 `renderTasks()` 方法中，使用链式 filter 表达式

**数据来源：**
- `buildMockTasks()` 内联 Mock 数据（4 条示例任务）
- 与 Today 页数据不共享，各自维护

**实现现状备注：**
- 三维度为 类别 / 象限 / 状态，**未实现验收清单 A3 要求的"任务类型（单次/长时）"与"日期范围"筛选控件**
- 代码内定义了 `TypeFilter { ALL, ONE_TIME, LONG }` 枚举但未接入 UI
- 列表项复用 `item_today_task.xml`，通过 `tvStepTag` 展示"N 个步骤"摘要（仅当 steps>0 显示）

### 4.4 TaskDetail 页面

**文件：** [TaskDetailActivity.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/tasks/TaskDetailActivity.kt)

**页面结构：**
- Toolbar：返回键 + 编辑/删除菜单
- 任务标题与标签区（分类、紧急、重要）
- 任务类型与基本信息
- 长时任务特有：累计用时、步骤列表、番茄钟入口
- 单次任务特有：不支持番茄钟的提示说明
- 备注区域

**核心功能：**
- 接收 Intent 参数（taskId、taskTitle、isLongTask 等）
- 根据任务类型（长时/单次）差异化渲染
- 长时任务渲染步骤列表（动态 addView 方式）
- 番茄钟入口跳转 PomodoroActivity
- 单次任务隐藏番茄钟入口并显示原因说明

**关键规则落点：**
- 单次任务隐藏 `btnPomodoro`，显示 `btnSingleTaskHint`
- 原因文案："单次任务不支持番茄钟计时"（strings.xml 中 `task_detail_no_pomodoro`）

**数据来源：**
- 步骤数据：`renderSteps()` 内联 5 条 Mock 步骤
- 任务描述和创建时间为写死的占位文案

**实现现状备注：**
- 启动 `PomodoroActivity` 时 **未传递 taskId / taskTitle 等参数**，番茄钟页无法获知当前任务上下文
- 编辑入口通过 Toolbar 菜单跳转 `TaskEditActivity`（见 4.5），为占位实现

### 4.5 TaskEdit 页面

**文件：** [TaskEditActivity.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/tasks/TaskEditActivity.kt)（独立 Activity，已在 AndroidManifest 注册）

**页面结构：**
- Toolbar：返回键 + 菜单
- 表单区：任务名称、类别、紧急、重要、任务类型（单次/长时）、计划时间
- 步骤编辑区：通过 `EditableStepAdapter` 动态增删步骤项

**核心功能：**
- 接收 Intent 参数（taskId，可空表示新建）
- 任务类型切换为长时时显示番茄钟参数配置区域（占位）
- 步骤列表的新增 / 删除（`EditableStep` + `EditableStepAdapter`）

**实现现状备注：**
- 为占位实现，保存按钮未接 Repository，未与列表页建立刷新回传
- 步骤数据来自 `buildMockSteps()` 内联 Mock
- 复用 `ui/common/` 下的 `EditableStep` 与 `EditableStepAdapter` 公共组件

### 4.6 Statistics 页面

**文件：**
- [StatisticsFragment.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/statistics/StatisticsFragment.kt)
- [StatisticsStackBarView.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/statistics/StatisticsStackBarView.kt)

**页面结构：**
- 顶部：标题 + 时间维度切换 Chip（UI 文案为"今日 / 本周 / 本月"）
- 类别筛选 Chip：全部类别 / 仅长时任务
- 统计卡片区：已完成数、进行中数、完成率（对应模块 B）
- 图表区：堆叠柱状图（自定义 View，对应模块 A）
- 注：模块 C（类别步骤完成时间）容器缺失

**核心功能：**
- 时间维度切换（见下方"实现现状备注"中的映射错位说明），对应不同 Mock 数据
- 类别筛选切换（全部 / 仅长时）
- 统计卡片展示关键指标
- 自定义堆叠柱状图展示时间段分布

**自定义 View 实现：**
`StatisticsStackBarView` 继承自 View，完全通过 Canvas 绘制：

- **数据模型**：`StatisticsBarItem`（X轴标签 + 多个分段），`StatisticsBarSegment`（名称 + 分钟数 + 颜色）
- **绘制内容**：水平网格线 + 基线 + 圆角矩形柱体 + X 轴标签
- **自适应逻辑**：
  - 柱宽 = (可用宽度 - 间隙总和) / 数据条数，设置最小宽度
  - 标签密度根据数据量自动调整步长（大于24条每4个显示，大于14条每2个显示）
- **主题适配**：网格线、基线、文字颜色通过 `resolveThemeColor()` 读取 Material 主题属性
- **更新方式**：`submitData()` 方法刷新数据后调用 `invalidate()` 重绘

**数据来源：**
- 三个时间维度各自硬编码 Mock 数据
- 单一系列颜色（橙色 `state_chart_orange`）

**实现现状备注：**
- **时间映射错位**：UI 文案为"今日 / 本周 / 本月"，但代码 `setupModes()` 将其映射到 `StatisticsMode.DAY / MONTH / YEAR` 枚举（标注 Placeholder），文案与实际模式不一致
- 类别筛选默认选中"全部类别"，**非"仅长时"**（与 A4 默认仅长时要求不符）
- 无独立"任务类型"筛选控件，仅以"全部类别 / 仅长时任务"间接表达
- **模块 C（类别步骤完成时间）容器缺失**，实际仅模块 A（图表卡）与模块 B（概览卡片）存在
- 无"仅统计长时任务 / 步骤"口径说明文案，无空态视图（与 A4 / A6 未勾项对应）

### 4.7 Settings 页面

**文件：** [SettingsFragment.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/settings/SettingsFragment.kt)

**页面结构（分组卡片式布局）：**
- 外观设置：主题模式
- 语言设置：应用语言
- 数据管理：导出数据
- 番茄钟设置：工作时长、休息时长、完成提醒开关
- 关于：版本号、检查更新

**核心功能：**
- 主题模式选择对话框（跟随系统/浅色/深色，单选列表）
  - 注意：MainActivity 强制浅色，当前选择不实际生效
- 语言设置：Toast "即将上线" 占位
- 数据导出：Toast 占位提示
- 番茄钟时长设置：数字输入对话框，带范围校验（1-120分钟）
- 完成提醒开关：Switch 切换
- 版本号：读取自 PackageManager
- 检查更新：Toast "已经是最新版本" 占位

**实现现状备注：**
- 数据管理区**仅有"导出数据"一个入口**，缺少验收清单 A5 要求的"导入"与"清空"入口（危险操作二次确认占位亦缺失）
- 主题模式选择因 MainActivity 强制浅色而**实际不生效**

**对话框实现：**
- 使用 `MaterialAlertDialogBuilder` 构建 Material 风格对话框
- 数字输入：动态创建 EditText，设置 inputType 为数字

### 4.8 Pomodoro 页面

**文件：** [PomodoroActivity.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/pomodoro/PomodoroActivity.kt)

**页面结构：**
- Toolbar：返回键
- 当前任务信息区（标题、描述、标签，占位显示）
- 番茄钟状态标签
- 倒计时显示 + 圆形进度条
- 操作按钮：开始/暂停/继续 + 重置

**核心功能：**
- 25分钟倒计时（`CountDownTimer` 实现）
- 状态机：IDLE / RUNNING / PAUSED 三态切换
- 进度条实时更新（ProgressBar）
- 计时结束自动回到 IDLE 状态
- 重置功能恢复初始时长

**状态切换逻辑：**
- IDLE → RUNNING：点击开始
- RUNNING → PAUSED：点击暂停
- PAUSED → RUNNING：点击继续
- 任意态 → IDLE：点击重置 / 计时结束

**资源管理：**
- `onDestroy()` 中取消 CountDownTimer，避免内存泄漏

**实现现状备注：**
- 计时硬编码 25 分钟（`totalTimeMs = 25 * 60 * 1000L`），**未读取设置页 `focusMinutes` 配置**
- 无 `isLongTask` 兜底校验：PomodoroActivity 自身不校验任务类型，若被外部直接唤起（异常路径）单次任务仍可进入，与 B4 要求不符
- 当前任务信息区为占位显示（TaskDetail 启动时未传参，无法展示真实任务）

---

## 5. 数据层现状

### 5.1 Room 数据库配置

**Gradle 依赖：**
```
room-runtime + room-ktx（implementation）
room-compiler（ksp）
```

**数据库实例：**
- 采用双重检查锁单例模式（Double-Checked Locking）
- `@Volatile` 修饰 INSTANCE 保证可见性
- `synchronized` 保证线程安全
- 使用 `applicationContext` 避免内存泄漏

### 5.2 现有实体与 DAO

**当前状态：**
- **AppInfo 实体**：表名 `app_info`，字段包括 `uid`（主键自增）、`db_version`（数据库版本）、`create_time`（创建时间戳）
- **AppInfoDao 接口**：`getLatestAppInfo()` 返回 LiveData，`insert(appInfo)` 插入记录
- **TaskEntity 实体**：表名 `task`，字段包括 `title`、`content`、`task_type`、`status`、`is_urgent`、`is_important`、`category_id`、`created_at`、`finished_at` 等，外键关联 Category，默认值为 0
- **TaskStepEntity 实体**：表名 `task_step`，字段包括 `task_id`、`title`、`sort_order`、`status`、`completed_at`、`spent_duration_sec` 等，外键级联删除 Task
- **CategoryEntity 实体**：表名 `category`，字段包括 `name`、`color_hex`、`icon_name`，名称唯一索引
- **PomodoroSessionEntity 实体**：表名 `pomodoro_session`，字段包括 `task_id`、`step_id`、`started_at`、`ended_at`、`focus_duration_sec`、`break_duration_sec`、`cycles`、`status`，外键关联 Task 和 TaskStep
- **DAO 接口**：TaskDao、TaskStepDao、CategoryDao、PomodoroSessionDao 均已完成，支持 Flow 查询和 suspend 操作

### 5.3 数据层架构（Stage B 已完成）

Stage B 已建立完整的 Repository 层架构：

**领域模型（domain/）：**
- `Task.kt`：任务领域模型
- `TaskStep.kt`：步骤领域模型
- `Category.kt`：分类领域模型
- `PomodoroSession.kt`：番茄钟会话领域模型
- `TaskType.kt`、`TaskStatus.kt`、`StepStatus.kt`、`SessionStatus.kt`：枚举类型

**Repository 接口（data/repository/）：**
- `TaskRepository`：任务 CRUD、筛选、状态流转（16 个方法）
- `CategoryRepository`：分类 CRUD（5 个方法）
- `PomodoroRepository`：番茄钟会话 CRUD（6 个方法）
- `TaskQuery`：组合查询条件数据类

**FakeRepository 实现（data/repository/）：**
- `FakeTaskRepository`：任务仓储内存实现
- `FakeCategoryRepository`：分类仓储内存实现
- `FakePomodoroRepository`：番茄钟仓储内存实现

**数据流链路：**
```
UI -> ViewModel -> Repository -> Flow/StateFlow -> ViewModel -> UI
```

**Stage B 核心价值：**
- 统一数据真相（Single Source of Truth）
- 建立响应式刷新链路（Reactive Data Flow）
- 完成 UI 职责收敛（UI Responsibility Reduction）
- 为 Room 落地做接口预埋（Room Migration Readiness）
- 多页面联动具备真实可扩展性（Scalability）

### 5.4 Stage C 计划

Stage C（已完成），所有步骤 C1-C8 均已实现：

1. **定义 Room Entity** ✅：TaskEntity/TaskStepEntity/CategoryEntity/PomodoroSessionEntity 已创建，配置外键约束和索引
2. **创建 DAO 接口** ✅：TaskDao/TaskStepDao/CategoryDao/PomodoroSessionDao 已创建，支持 Flow 查询和 suspend 操作
3. **更新 AppDatabase** ✅：注册新表（5 张表），版本号保持为 1（开发阶段使用 fallbackToDestructiveMigration）
4. **创建 Entity-Domain 映射工具** ✅：EntityMapper.kt 使用扩展函数实现双向转换，使用 @JvmName 解决类型擦除冲突
5. **实现 RoomRepository** ✅：RoomTaskRepository/RoomCategoryRepository/RoomPomodoroRepository 已完成，实现所有接口方法
6. **创建数据初始化工具** ✅：DatabaseInitializer.kt 支持默认分类（5条）、演示任务（6条）、关联步骤（8条）的自动初始化
7. **切换 ViewModelFactory 依赖** ✅：已完成，将 FakeRepository 替换为 RoomRepository
8. **验证与测试** ✅：项目编译通过，数据库表结构验证测试已完成

详细计划与进度详见 [StageC_轻量数据结构落地计划](file:///c:/code/MyTodo/docs/plan/stageC/StageC_轻量数据结构落地计划.md)

---

## 6. UI 设计规范

### 6.1 Material 3 主题配置

**主题基类：** `Theme.Material3.Light.NoActionBar`

**主题文件：** [themes.xml](file:///c:/code/MyTodo/app/src/main/res/values/themes.xml)

**主题色板：**

| 角色 | 颜色 | 色值 |
|------|------|------|
| colorPrimary | brand_indigo | #4F46E5（靛蓝） |
| colorPrimaryContainer | brand_fab_bg | #E8EAF6（浅靛蓝） |
| colorOnPrimary | white | #FFFFFF |
| colorSecondary | teal_700 | #167C74（青绿色） |
| android:colorBackground | page_bg_light | #FAFAFA（浅灰） |
| colorSurface | card_bg_white | #FFFFFF（纯白） |
| colorOnSurface | text_primary | #1A1A1A（近黑） |
| colorError | state_error_red | #EF4444（正红） |

**自定义组件样式：**
- `Widget.MyPotato.CheckBox`：自定义圆形复选框样式
  - 使用自定义 drawable `sel_checkbox_circle`
  - 取消 Material 默认着色（buttonTint = @null）

### 6.2 颜色系统

**颜色定义文件：** [colors.xml](file:///c:/code/MyTodo/app/src/main/res/values/colors.xml)

**分类标签颜色体系：**

| 分类 | 背景色 | 文字色 |
|------|--------|--------|
| 工作 | #F3E8FF（浅紫） | #1A1A1A |
| 购物/生活 | #FFEDD5（浅橙） | #C2410C |
| 学习 | #DBEAFE（浅蓝） | #1D4ED8 |
| 健康 | #FCE7F3（浅粉） | #BE123C |
| 默认 | #F3F4F6（浅灰） | #4B5563 |

**状态色：**
- 成功：#10B981（绿色）
- 错误：#EF4444（红色）
- 图表：#F59E0B（橙色）

### 6.3 自定义组件

**自定义 View：**
- [StatisticsStackBarView](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/statistics/StatisticsStackBarView.kt)：堆叠柱状图

**自定义 Drawable：**
- `bg_tag_rounded.xml` / `bg_tag_outline.xml` / `bg_tag_type.xml`：各类标签背景
- `bg_segmented_container.xml` / `bg_segmented_item_checked.xml`：分段控制器背景
- `sel_checkbox_circle.xml`：圆形复选框选择器
- `chip_bg_color_selector.xml` / `chip_stroke_color_selector.xml`：Chip 颜色选择器

---

## 7. 核心技术点

### 7.1 ViewBinding

**启用配置：** build.gradle 中 `buildFeatures { viewBinding = true }`

**使用模式：**
- Fragment：`FragmentXxxBinding.inflate(inflater, container, false)`，`onDestroyView()` 中置空 `_binding`
- Activity：`ActivityXxxBinding.inflate(layoutInflater)`
- 自定义布局（如 BottomSheet）：`XxxBinding.bind(contentView)`

**优点：**
- 类型安全，编译时检查
- 避免 findViewById 模板代码
- 空安全（通过 `_binding` 可空属性 + `binding` 非空属性的模式）

### 7.2 RecyclerView 适配器模式

**统一模式：**
- 适配器为 Fragment 内部私有类
- 通过构造函数注入点击回调 lambda
- ViewHolder 使用 ViewBinding
- `submitData()` 方法更新数据 + `notifyDataSetChanged()`

**局限：**
- 使用 `notifyDataSetChanged()` 全量刷新，无动画
- 未使用 DiffUtil 进行增量更新
- 未使用 ListAdapter

### 7.3 自定义 View 绘制

`StatisticsStackBarView` 展示了完整的自定义 View 绘制流程：

1. **定义 Paint**：网格线、基线、柱体、标签各一支 Paint
2. **onDraw 流程**：
   - 更新主题颜色
   - 计算绘图区域（考虑 padding）
   - 绘制网格（4 条水平分割线 + 1 条基线）
   - 遍历数据绘制柱体（从下往上堆叠，圆角矩形）
   - 绘制 X 轴标签（自适应密度）
3. **尺寸工具**：dp() / sp() 扩展方法
4. **数据驱动**：submitData() -> invalidate() -> onDraw()

### 7.4 BottomSheetDialog 使用

**创建流程：**
1. `BottomSheetDialog(context)` 创建实例
2. `layoutInflater.inflate()` 加载布局
3. `BottomSheetXxxBinding.bind()` 获取绑定
4. 绑定控件事件
5. `dialog.setContentView(content)` 设置内容
6. `dialog.show()` 显示

**应用场景：**
- Today 页 FAB 新建任务
- Tasks 页 FAB 新建任务
- 两页面复用同一个布局文件 `bottom_sheet_add_task_placeholder.xml`

---

## 8. 当前局限与后续规划

### 8.1 已知问题

| 问题 | 影响 | 优先级 | 状态 |
|------|------|--------|------|
| MainActivity 强制浅色模式 | 主题切换功能无效 | 中 | 待修复 |
| 各页面 Mock 数据分散重复 | 数据不一致，维护成本高 | 高 | ✅ Stage B 已解决 |
| Task Detail / Pomodoro 使用独立 Activity | 与主页面数据共享困难 | 中 | 待优化 |
| Statistics 页无"仅统计长时任务"口径说明 | 关键规则缺失 | 高 | 待修复 |
| Settings 页缺少导入/清空入口 | 功能不完整 | 中 | 待修复 |
| Today 页筛选维度与验收清单不一致 | 需求偏差 | 高 | 待修复 |
| Statistics 时间维度映射错位（今日/本周/本月 → DAY/MONTH/YEAR） | 文案与模式不一致 | 中 | 待修复 |
| Statistics 模块 C（类别步骤完成时间）容器缺失 | 与大纲三模块定义不符 | 高 | 待修复 |
| Statistics 类别筛选默认"全部"而非"仅长时" | 与 A4 默认仅长时要求不符 | 高 | 待修复 |
| TaskDetail 启动 Pomodoro 未传任务参数 | 番茄钟页无法获知任务上下文 | 中 | ✅ Stage B 已解决 |
| Pomodoro 计时未读取设置页 focusMinutes | 设置不生效 | 中 | ✅ Stage B 已解决 |
| Pomodoro 无 isLongTask 兜底校验 | 异常路径单次任务可进入 | 高 | ✅ Stage B 已解决 |
| RecyclerView 使用 notifyDataSetChanged | 性能较差，无动画 | 低 | 待优化 |

### 8.2 技术债

| 技术债 | 说明 | 状态 |
|--------|------|------|
| 缺少 Repository 层 | 数据访问直接在 Fragment 中硬编码，后续接入 Room 改动面大 | ✅ Stage B 已解决 |
| 缺少统一数据模型 | TodayTask、TaskUiModel 等多个数据类字段重复 | ✅ Stage B 已解决 |
| 导航未使用 Safe Args | Intent 传参类型不安全，容易写错 key | ✅ Stage B 已解决 |
| 缺少 ViewModel 分层 | 各 Fragment 自行管理状态，未使用 ViewModel 保存状态 | ✅ Stage B 已解决 |
| 深色模式未适配 | 仅有 values-night/themes.xml 占位，MainActivity 强制浅色 | 待修复 |
| 无单元测试/UI 测试 | 测试覆盖率为 0 | 待修复 |
| 数据未持久化 | 当前使用 FakeRepository 内存实现，重启后数据丢失 | ✅ Stage C 已解决（RoomRepository 已切换） |

### 8.3 Stage B 完成情况

Stage B 已全部完成，实现了：

1. **引入 Repository 接口 + FakeRepository**：统一数据来源，各页面从同一仓库取数
2. **定义统一领域模型**：Task/TaskStep/Category/PomodoroSession 等 8 个领域模型
3. **核心流程闭环**：新建任务 → 保存 → 列表刷新；编辑任务 → 保存 → 详情刷新；标记完成 → 状态即时可见
4. **导航参数规范化**：全链路使用 Safe Args 参数协议
5. **规则一致性校验**：单次任务在 TaskDetailActivity 和 PomodoroViewModel 双重校验下无法进入番茄钟
6. **ViewModel 体系补齐**：TodayViewModel/TasksViewModel/TaskDetailViewModel/TaskEditViewModel/PomodoroViewModel/SettingsViewModel 全部落地

### 8.4 Stage C 已完成

参考 [StageC_轻量数据结构落地计划](file:///c:/code/MyTodo/docs/plan/stageC/StageC_轻量数据结构落地计划.md)：

**已完成（步骤 C1-C8）：**
1. **定义 Room Entity** ✅：TaskEntity/TaskStepEntity/CategoryEntity/PomodoroSessionEntity 已创建，配置外键约束和索引
2. **创建 DAO 接口** ✅：TaskDao/TaskStepDao/CategoryDao/PomodoroSessionDao 已创建，支持 Flow 查询和 suspend 操作
3. **更新 AppDatabase** ✅：注册 5 张表，版本号保持为 1（开发阶段）
4. **创建 Entity-Domain 映射工具** ✅：EntityMapper.kt 使用扩展函数实现双向转换
5. **实现 RoomRepository** ✅：3 个 RoomRepository 已完成，实现所有接口方法
6. **创建数据初始化工具** ✅：DatabaseInitializer.kt 支持默认数据自动初始化
7. **切换 ViewModelFactory 依赖** ✅：已将 FakeRepository 替换为 RoomRepository
8. **验证与测试** ✅：项目编译通过，数据库表结构验证测试已完成

---

## 9. 阶段验收对照（Stage A）

本章将《UI骨架验收清单.md》Stage A 全部验收项与工程实际实现逐项对照，客观记录勾选状态与实际偏差，**不下阶段进入性结论**。

### 9.1 验收概述

- Stage A 共 21 个子项：**已勾选 14 项**（其中 1 项存在实现偏差），**未勾选 7 项**。
- 已勾但有偏差的项：A4「模块 A/B/C 的容器结构存在」（模块 C 缺失，详见 9.3）。
- 未勾选项均与实际实现一致，详见 9.4。

### 9.2 Stage A 逐项对照表

| 编号 | 验收项 | 勾选 | 实际实现 | 一致性 |
|------|--------|------|----------|--------|
| A1.1 | 底部导航 4 入口（Today/Tasks/Statistics/Settings） | ✅ | 4 入口齐全 | 一致 |
| A1.2 | 顶部 AppBar 标题一致 | ✅ | 各 Fragment 标题一致 | 一致 |
| A1.3 | 深层页面返回路径可预期 | ✅ | TaskDetail/TaskEdit/Pomodoro 均有 Toolbar 返回 | 一致 |
| A2.1 | 日期 + 问候语区域 | ✅ | 已实现 | 一致 |
| A2.2 | 快速筛选 全部 / 单次 / 长时 | ⬜ | 实际为四象限维度，无单次/长时 | 一致（未完成） |
| A2.3 | 任务列表按四象限分组 | ⬜ | 平铺渲染（按 done 排序），未分组 | 一致（未完成） |
| A2.4 | FAB 新建入口 + 占位反馈 | ✅ | FAB + BottomSheet 占位 | 一致 |
| A2.5 | 空态 + 新建引导 | ✅ | 空态文案 + 引导 | 一致 |
| A3.1 | 顶部 Tab：类别 / 象限 / 状态 | ✅ | 分段控制器三维度 | 一致 |
| A3.2 | 筛选区：类别 / 任务类型 / 日期范围 | ⬜ | 缺任务类型与日期范围 | 一致（未完成） |
| A3.3 | 列表项步骤摘要区域 | ✅ | tvStepTag 展示步骤数 | 一致 |
| A3.4 | 空态 + 引导 | ✅ | 空态文案 + 引导 | 一致 |
| A4.1 | 顶部筛选 日/周/月、类别、任务类型（默认仅长时） | ⬜ | 时间文案今日/本周/本月映射错位；类别默认全选；无独立任务类型 | 一致（未完成） |
| A4.2 | 模块 A/B/C 容器结构存在 | ✅ | **偏差**：仅 A、B 存在，模块 C 缺失 | **偏差** |
| A4.3 | 模块 A 空态/说明"仅统计长时任务/步骤" | ⬜ | 无口径说明，无空态 | 一致（未完成） |
| A5.1 | 主题模式入口（跟随系统/浅色/深色） | ✅ | 入口存在（但 MainActivity 强制浅色，不生效） | 一致 |
| A5.2 | 语言入口（跟随系统/简中/English） | ✅ | 入口存在（Toast 占位） | 一致 |
| A5.3 | 数据管理 导入/导出/清空 | ⬜ | 仅导出一个入口 | 一致（未完成） |
| A5.4 | 关于（版本号、检查更新） | ✅ | 已实现占位 | 一致 |
| A6.1 | 单次任务不可启动番茄钟 + 原因说明 | ✅ | 隐藏按钮 + 显示原因文案 | 一致 |
| A6.2 | Statistics 模块 A 口径"单次任务不纳入统计" | ⬜ | 无口径表达 | 一致（未完成） |

### 9.3 已勾偏差项说明

**A4.2「模块 A/B/C 的容器结构存在」**
- 验收要求：模块 A（每日时间段分布）、模块 B（完成情况统计）、模块 C（类别步骤完成时间）三模块容器结构存在。
- 实际实现：仅模块 A（`StatisticsStackBarView` 图表卡）与模块 B（已完成数 / 进行中数 / 完成率概览卡）存在，**模块 C 容器缺失**。
- 处置：勾选状态保留，已在验收清单该项追加偏差备注。

### 9.4 未完成项清单（7 项）

| 编号 | 缺口简述 |
|------|----------|
| A2.2 | Today 顶部缺"全部 / 单次 / 长时"快速筛选（现为四象限维度） |
| A2.3 | Today 任务列表未按四象限分组（现平铺渲染） |
| A3.2 | Tasks 缺"任务类型"与"日期范围"筛选控件 |
| A4.1 | Statistics 时间维度映射错位 + 类别默认非仅长时 + 无独立任务类型筛选 |
| A4.3 | Statistics 模块 A 无"仅统计长时任务/步骤"口径说明与空态 |
| A5.3 | Settings 数据管理缺"导入"与"清空"入口（含二次确认占位） |
| A6.2 | Statistics 无"单次任务不纳入统计"口径表达 |

> 注：A4.3 与 A6.2 同属 Statistics 模块 A 口径表达缺口，本质为同一规则的两处落点。

---

## 附录：核心文件索引

| 文件 | 路径 |
|------|------|
| 主 Activity | [MainActivity.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/MainActivity.kt) |
| 共享 ViewModel | [MainViewModel.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/MainViewModel.kt) |
| Today 页面 | [TodayFragment.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/today/TodayFragment.kt) |
| Tasks 页面 | [TasksFragment.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/tasks/TasksFragment.kt) |
| 任务详情 | [TaskDetailActivity.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/tasks/TaskDetailActivity.kt) |
| 任务编辑 | [TaskEditActivity.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/tasks/TaskEditActivity.kt) |
| 统计页面 | [StatisticsFragment.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/statistics/StatisticsFragment.kt) |
| 柱状图自定义 View | [StatisticsStackBarView.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/statistics/StatisticsStackBarView.kt) |
| 设置页面 | [SettingsFragment.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/settings/SettingsFragment.kt) |
| 番茄钟页面 | [PomodoroActivity.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/ui/pomodoro/PomodoroActivity.kt) |
| Room 数据库 | [AppDatabase.kt](file:///c:/code/MyTodo/app/src/main/java/com/gordon/mypotato/data/AppDatabase.kt) |
| 主题配置 | [themes.xml](file:///c:/code/MyTodo/app/src/main/res/values/themes.xml) |
| 颜色定义 | [colors.xml](file:///c:/code/MyTodo/app/src/main/res/values/colors.xml) |
| 字符串资源 | [strings.xml](file:///c:/code/MyTodo/app/src/main/res/values/strings.xml) |
| 导航图 | [main_nav_graph.xml](file:///c:/code/MyTodo/app/src/main/res/navigation/main_nav_graph.xml) |
| 底部导航菜单 | [bottom_nav_menu.xml](file:///c:/code/MyTodo/app/src/main/res/menu/bottom_nav_menu.xml) |
| 应用构建配置 | [app/build.gradle](file:///c:/code/MyTodo/app/build.gradle) |
| 依赖版本管理 | [libs.versions.toml](file:///c:/code/MyTodo/gradle/libs.versions.toml) |
