<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- App theme entry: use unified Material3 base theme. -->
    <style name="Theme.MyPotato" parent="Base.Theme.MyPotato" />

    <!-- Base application theme (Material3 - dark). -->
    <style name="Base.Theme.MyPotato" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorSurface">@color/black</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="android:statusBarColor">?attr/colorSurface</item>
    </style>
</resources>
